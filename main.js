//JS functions, could have been done inside the QML code but
//i expected to write more than I did

function clicked()
{
    return colourPicker.textInput.text
}

function clickedb()
{
    return xTextInput.textInput.text
}

function clickedc()
{
    return yTextInput.textInput.text
}
